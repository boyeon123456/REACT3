FROM node:22-alpine

WORKDIR /app

COPY backend/package*.json ./backend/
RUN cd backend && npm ci --omit=dev

COPY backend ./backend
COPY src/constants/defaultShopItems.json ./src/constants/defaultShopItems.json

ENV NODE_ENV=production
WORKDIR /app/backend

EXPOSE 3000
CMD ["npm", "start"]
